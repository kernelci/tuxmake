[target]
description = "Binary RPM packages"
dependencies = default
commands = {make} binrpm-pkg && sh -c 'find {build_dir}/rpmbuild/RPMS -name "*.rpm" -exec mv {{}} {build_dir} \; 2>/dev/null || mv {build_dir}/../*.rpm {build_dir} 2>/dev/null || true'

[artifacts]
kernel-*.rpm = kernel-*.rpm
